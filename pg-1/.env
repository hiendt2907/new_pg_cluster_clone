# PostgreSQL Node Configuration for Railway
# NODE_NAME: Tên node (bắt buộc, vì Railway hostname là random)
NODE_NAME=pg-1

# NODE_ID: ID duy nhất cho node (bắt buộc, phải là số)
NODE_ID=1

# PEERS: Các node khác trong cluster (dùng .railway.internal cho private network)
PEERS=pg-2.railway.internal,pg-3.railway.internal

# Shared variables (POSTGRES_PASSWORD, REPMGR_PASSWORD, PRIMARY_HINT) 
# sẽ được set ở environment level, không cần khai báo ở đây
